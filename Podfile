# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ios-vehicle-moving' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ios-vehicle-moving

  target 'ios-vehicle-movingTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ios-vehicle-movingUITests' do
    # Pods for testing
  end

end
